{
  "dependencies": {
    "axios": "^1.3.5",
    "mongoose": "^7.0.3"
  },
  "name": "react-new1",
  "version": "1.0.0",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server":"pnpm run server --cwd backend",
    "client":"pnpm run dev --prefix vite-project",
    "build":"pnpm build --prefix vite-project",
    "start":"pnpm run server",
    "install_master": "pnpm install & pnpm install_client & pnpm install_server",
    "install_client":"pnpm install --prefix vite-project",
    "install_server":"pnpm install --prefix backend"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
